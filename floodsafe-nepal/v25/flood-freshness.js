(()=>{'use strict';
if(window.__fsImpactFreshV5)return;window.__fsImpactFreshV5=true;
const $=id=>document.getElementById(id),RAW='https://raw.githubusercontent.com/Pujan1234-hub/assigment/main/data/floodsafe-people-status.json',LOCAL='../../data/floodsafe-people-status.json';let last=0,current=null;
const lang=()=>window.FloodSafe?.state?.lang||localStorage.getItem('fs-flood-lang')||'ne',tr=(ne,en)=>lang()==='en'?en:ne;
const ts=v=>{const n=+new Date(v||0);return Number.isFinite(n)?n:0};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function get(u,ms=9000){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(u+(u.includes('?')?'&':'?')+'_if5='+Date.now(),{cache:'no-store',credentials:'omit',signal:c.signal});if(!r.ok)throw Error(r.status);return await r.json()}finally{clearTimeout(t)}}
async function getLatest(){try{return await get(RAW)}catch{return await get(LOCAL)}}
function fmt(t){if(!ts(t))return'—';try{return new Intl.DateTimeFormat(lang()==='en'?'en-GB':'ne-NP',{timeZone:'Asia/Kathmandu',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date(t))+' NPT'}catch{return String(t)}}
function sourceBox(parentId,source,url,time){const p=$(parentId)?.closest('.impactStat');if(!p)return;let e=p.querySelector('.impactSource');if(!e){e=document.createElement('div');e.className='impactSource';p.appendChild(e)}const label=tr('स्रोत','Source'),timeLabel=tr('समय','Time'),link=url?`<a href="${esc(url)}" target="_blank" rel="noopener noreferrer">${esc(source||'—')}</a>`:esc(source||'—');e.innerHTML=`${label}: ${link}${time?`<br>${timeLabel}: ${esc(fmt(time))}`:''}`}
function apply(j){current=j;const d=j?.recovered_bodies??null,m=j?.missing_minimum??null,r=j?.rescued_alive??null,metricTimes=[j?.recovered_update_time,j?.missing_update_time,j?.rescued_update_time].map(ts).filter(Boolean),newest=metricTimes.sort((a,b)=>b-a)[0]||0,checked=ts(j?.last_checked_utc);if($('impactDeaths'))$('impactDeaths').textContent=d??'—';if($('impactMissing'))$('impactMissing').textContent=m??'—';if($('impactRescued'))$('impactRescued').textContent=r??'—';if($('impactEvent'))$('impactEvent').textContent=lang()==='en'?(j?.event||'Bhotekoshi flash flood'):(j?.event_ne||j?.event||'भोटेकोशी आकस्मिक बाढी');if($('impactFresh'))$('impactFresh').textContent=newest?tr(`पछिल्लो प्रमाणित आँकडा: ${fmt(newest)} • स्रोत जाँच: ${checked?fmt(checked):'—'}`,`Latest verified figures: ${fmt(newest)} • source scan: ${checked?fmt(checked):'—'}`):tr('पछिल्लो प्रमाणित आँकडा जाँच हुँदैछ','Checking latest verified figures');sourceBox('impactDeaths',j?.recovered_source,j?.recovered_source_url,j?.recovered_update_time);sourceBox('impactMissing',j?.missing_source,j?.missing_source_url,j?.missing_update_time);sourceBox('impactRescued',j?.rescued_source,j?.rescued_source_url,j?.rescued_update_time);if($('impactDetail')){const src=Array.isArray(j?.sync_sources)?j.sync_sources.join(' • '):'';$('impactDetail').textContent=tr(`स्वचालित स्रोत जाँच: ${src||'नेपाल प्रहरी • रेडियो नेपाल • RONB • Hamro Patro'}`,`Automatic source scan: ${src||'Nepal Police • Radio Nepal • RONB • Hamro Patro'}`)}}
async function sync(force=false){if(!force&&Date.now()-last<20000)return;last=Date.now();try{apply(await getLatest())}catch{if($('impactFresh'))$('impactFresh').textContent=tr('मानवीय असरको स्रोत अहिले उपलब्ध छैन','Human-impact source is currently unavailable')}}
function boot(){sync(true);setInterval(sync,30000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync(true)});window.addEventListener('online',()=>sync(true));window.addEventListener('fslanguage',()=>{if(current)apply(current)})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();