(()=>{'use strict';
if(window.__fsImpactFreshV4)return;window.__fsImpactFreshV4=true;
const $=id=>document.getElementById(id),PEOPLE='https://raw.githubusercontent.com/Pujan1234-hub/assigment/main/data/floodsafe-people-status.json';let last=0,current=null;
const lang=()=>window.FloodSafe?.state?.lang||localStorage.getItem('fs-flood-lang')||'ne',tr=(ne,en)=>lang()==='en'?en:ne;
const ts=v=>{const n=+new Date(v||0);return Number.isFinite(n)?n:0};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function get(u,ms=9000){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(u+(u.includes('?')?'&':'?')+'_if4='+Date.now(),{cache:'no-store',credentials:'omit',signal:c.signal});if(!r.ok)throw Error(r.status);return await r.json()}finally{clearTimeout(t)}}
function fmt(t){if(!ts(t))return'—';try{return new Intl.DateTimeFormat(lang()==='en'?'en-GB':'ne-NP',{timeZone:'Asia/Kathmandu',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(t))+' NPT'}catch{return String(t)}}
function sourceBox(parentId,source,url){const p=$(parentId)?.closest('.impactStat');if(!p)return;let e=p.querySelector('.impactSource');if(!e){e=document.createElement('div');e.className='impactSource';p.appendChild(e)}const label=tr('स्रोत','Source');e.innerHTML=url?`${label}: <a href="${esc(url)}" target="_blank" rel="noopener noreferrer">${esc(source||'—')}</a>`:`${label}: ${esc(source||'—')}`}
function apply(j){current=j;const d=j?.recovered_bodies??null,m=j?.missing_minimum??null,r=j?.rescued_alive??null,newest=[j?.recovered_update_time,j?.missing_update_time,j?.rescued_update_time,j?.updated_date].map(ts).filter(Boolean).sort((a,b)=>b-a)[0]||0;if($('impactDeaths'))$('impactDeaths').textContent=d??'—';if($('impactMissing'))$('impactMissing').textContent=m??'—';if($('impactRescued'))$('impactRescued').textContent=r??'—';if($('impactEvent'))$('impactEvent').textContent=lang()==='en'?(j?.event||'Bhotekoshi flash flood'):(j?.event_ne||j?.event||'भोटेकोशी आकस्मिक बाढी');if($('impactFresh'))$('impactFresh').textContent=newest?tr(`अपडेट • ${fmt(newest)} • स्रोत हरेक ५ मिनेटमा जाँच`,`Updated • ${fmt(newest)} • sources checked every 5 min`):tr('पछिल्लो अपडेट','Latest update');sourceBox('impactDeaths',j?.recovered_source,j?.recovered_source_url);sourceBox('impactMissing',j?.missing_source,j?.missing_source_url);sourceBox('impactRescued',j?.rescued_source,j?.rescued_source_url);if($('impactDetail'))$('impactDetail').textContent=j?.last_checked_utc?tr(`अन्तिम स्रोत जाँच: ${fmt(j.last_checked_utc)}`,`Last source check: ${fmt(j.last_checked_utc)}`):''}
async function sync(force=false){if(!force&&Date.now()-last<20000)return;last=Date.now();try{apply(await get(PEOPLE))}catch{if($('impactFresh'))$('impactFresh').textContent=tr('मानवीय असर data refresh उपलब्ध छैन','Impact data refresh unavailable')}}
function boot(){sync(true);setInterval(sync,30000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync(true)});window.addEventListener('online',()=>sync(true));window.addEventListener('fslanguage',()=>{if(current)apply(current)})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();