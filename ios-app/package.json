{
  "name": "floodsafe-nepal-ios",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:web": "node prepare-web.mjs",
    "ios:add": "npm run prepare:web && npx cap add ios",
    "ios:sync": "npm run prepare:web && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
